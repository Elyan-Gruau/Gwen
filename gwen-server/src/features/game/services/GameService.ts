export class GameService {
  public async createGame(player1Id: string, player2Id: string): Promise<{ _id: string }> {
    // TODO: Save a game to the database
    // TODO: Get the UserFactionDecks of the players
    // TODO: Initialize the board
    // TODO: Return game id
    return { _id: 'temp-game-id' };
  }
}
