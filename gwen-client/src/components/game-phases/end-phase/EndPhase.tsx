const EndPhase = () => {
  /*const roundResults = {};
    const winStatus = WIN_STATUS.WIN;




    return (
        <div>
        <span>
            {winStatus === WIN_STATUS.WIN ? 'You win!' : winStatus === WIN_STATUS.LOSS ? 'You lose!' : 'Draw!'}
        </span>
        </div>
    );*/
};

export default EndPhase;
